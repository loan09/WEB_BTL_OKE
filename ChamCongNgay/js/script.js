
function addToCart(id) {
    countInt = +count.innerText;
    countInt++;
    count.innerText = countInt;
}

function addToCart1(id) {
    countInt = +count1.innerText;
    countInt++;
    count1.innerText = countInt;
}
function addToCart2(id) {
    countInt = +count2.innerText;
    countInt++;
    count2.innerText = countInt;
}

function addToCart3(id) {
    countInt = +count3.innerText;
    countInt++;
    count3.innerText = countInt;
}
function addToCart4(id) {
    countInt = +count4.innerText;
    countInt++;
    count4.innerText = countInt;
}
function addToCart5(id) {
    countInt = +count5.innerText;
    countInt++;
    count5.innerText = countInt;
}
function save()
{
    alert("Đã lưu!");
}

